<ul class="ui-helper-border popover-blue-shadow">

	{{#if notificationsType}}
		{{#elements}}
	<li>
		<div {{#if moreInfo}} class="message-with-button" {{/if}}>
			<p class=" popover-title notification-title">{{title}}</p>
			<p class="notification-message">{{message}}</p>
			<p class="notification-time">Time: {{time}}</p>
		</div>
		{{#if moreInfo}}
		<div class="more-info-buttons">
			<button class="notifications-blue-button">NEW</button>
			<button class="white-button notifications-button">More Info</button>
		</div>
		{{/if}}
	</li>
		{{/elements}}
	{{/if}}

	{{#if menuType}}
		{{#elements}}
	<li class="popover-menu-option {{#if innerMenu}} inner-menu-activation-zone {{else}} inner-menu-deactivation-zone {{/if}}">
			<p class=" popover-title menu-option-title">{{title}}</p>
			{{#if innerMenu}}
			<span class="sprite-black-right-arrow-ico ui-icon right popover-footer-arow-position"></span>

			<ul class="ui-helper-border ui-helper-hidden inner-menu popover-blue-shadow">
				<li>
					<span>Operations View</span>
				</li>
				<li>
					<span>Alert Dashboard</span>
				</li>
				<li>
					<span>Process Editor</span>
				</li>
				<li>
					<span>Policy Editor</span>
				</li>
				<li>
					<span>Model Relationship Editor</span>
				</li>
			</ul>
			{{/if}}
	</li>
		{{/elements}}
	{{/if}}

	<li class="popover-footer  inner-menu-deactivation-zone">
		{{#if notificationsType}}
		<span class="popover-footer-icon sprite-alert-ico ui-icon left"></span>
		<span class="popover-footer-title">Alerts notifications</span>
		<a href class="notification-message right">See All</a>
		{{/if}}

		{{#if menuType}}
		<span class="popover-footer-icon sprite-gray-toolbox-ico ui-icon left"></span>
		<span class="popover-footer-title">Toolbox menu</span>
		{{/if}}
		<div class="triangle-down-shadow"></div>
		<div class="triangle-down-border"></div>
		<div class="triangle-down"></div>
	</li>
</ul>